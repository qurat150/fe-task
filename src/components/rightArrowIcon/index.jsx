import { Icons } from 'assets';
import React from 'react';

const RightArrowIcon = () => {
  return (
    <img
      style={{
        width: '13px',
        height: '13px',
        margin: '0 10px 0 10px',
      }}
      src={Icons.page2vectorIcon.src}
      alt={Icons.page2vectorIcon.alt}
    />
  );
};

export default RightArrowIcon;
